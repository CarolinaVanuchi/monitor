services:
  cpp:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    tty: true
    stdin_open: true
    command: >
      bash -lc "cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug
      && cmake --build build
      && ./build/app"